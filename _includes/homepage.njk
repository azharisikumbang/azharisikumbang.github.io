---
layout: base.njk
---
<header>
    <h1 style="margin: 0px">
        <a href="{{ site.url }}/about">{{ homepage.title or site.title }}</a>
    </h1>

    {% if homepage.show_subtitle %}
        {% set subtitle = homepage.subtitle %}
        {% if subtitle %}
            <p style="margin-top: 0px">{{ subtitle }}</p>
        {% endif %}
    {% endif %}
    <div id="nav">
        <a href="{{ site.url }}">beranda</a> -
        <a href="{{ site.url }}/project/">project</a> -
        <a href="{{ site.url }}/about/">tentang saya</a>
    </div>
</header>
<main>
    <hr>
    {% set medium_entries = medium %}
    {% include "medium.njk" %}

    {% set entries = homepage.menu %}
    {% include "menu.njk" %}
</main>
